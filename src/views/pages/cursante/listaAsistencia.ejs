<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Asistencia - <%= cursoLocal.idOfertaOficial %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Configuración General */
        body { background-color: white; color: black; font-family: Arial, sans-serif; }
        .font-small { font-size: 0.85rem; }

        /* Tabla Estilo Técnico para Impresión */
        .tabla-print { border-collapse: collapse; width: 100%; }
        .tabla-print th, .tabla-print td { 
            border: 1px solid #000 !important; 
            padding: 4px 6px; 
            vertical-align: middle;
        }

        /* Contenedor de Título con Recorte Inteligente */
        .header-titulo-container {
            display: flex;
            align-items: baseline;
            overflow: hidden;
            flex: 1;
            min-width: 0;
        }

        .titulo-recortado {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            margin-bottom: 0;
        }

        /* Configuración de Impresión */
        /* @media print {
            .no-print { display: none; }
            @page { 
                size: <%= cursoLocal.cantidadEncuentros > 6 ? 'landscape' : 'portrait' %>; 
                margin: 1cm; 
            }
            body { margin: 0; }
            thead { display: table-header-group; }
        } */
    </style>
</head>
<body onload="window.print()">

    <div class="container-fluid py-2">
        
        <div class="header-wrapper">
            <div class="d-flex justify-content-between align-items-end gap-3 mb-1">
                <div class="header-titulo-container">
                    <h5 class="titulo-recortado">
                        <strong>
                            <%= cursoLocal.cargoId.areaId.nombreCorto%>
                            <span class="text-muted fw-normal small"><%= cursoLocal.anio %>CH<%= cursoLocal.cohorte %></span>
                        </strong> 
                        <span class="text-muted small"><%= cursoLocal.cargoId.comision>1?(cursoLocal.cargoId.comision):'' %></span>: 
                        <span class="text-muted fw-normal small"><%= cursoLocal.nombrePropuesta %></span>
                    </h5>
                </div>
                
                <div class="text-end flex-shrink-0">
                    <div class="text-uppercase fw-bold text-muted" style="font-size: 9px; margin-top: 2px;">
                        Sistema SICAA
                    </div>
                </div>
            </div>

            <div class="font-small pb-1">
                <strong>ID ABC:</strong> <%= cursoLocal.idOfertaOficial %> | 
                <strong>Formador:</strong> <%= cursoLocal.formadorAbc %> |
                <strong>CIIE:</strong> <%= cursoLocal.cargoId?.ciieId?.nombre || 'Distrital' %> 
            </div>

            <hr class="mt-0 mb-3" style="border-top: 2px solid #000; opacity: 1;">
        </div>

        <table class="tabla-print">
            <thead>
                <tr class="bg-light">
                    <th rowspan="2" style="width: 30%;">Apellido y Nombre</th>
                    <th rowspan="2" class="text-center" style="width: 12%;">DNI</th>
                    <th colspan="<%= cursoLocal.cantidadEncuentros %>" class="text-center">Encuentros / Fechas</th>
                    <th rowspan="2" class="text-center" style="width: 15%;">Observaciones</th>
                </tr>
                <tr class="bg-light">
                    <% for(let j=1; j <= cursoLocal.cantidadEncuentros; j++) { %>
                        <th class="text-center" style="width: 45px;"><small><%= j %>°</small></th>
                    <% } %>
                </tr>
            </thead>
            <tbody>
                <% inscriptosLocales.forEach((ins, index) => { %>
                    <tr>
                        <td class="text-uppercase font-small">
                            <strong><%= index + 1 %>)</strong> <%= ins.apellido %>, <%= ins.nombres %>
                        </td>
                        <td class="text-center font-small"><%= ins.dni %></td>
                        
                        <% for(let j=1; j <= cursoLocal.cantidadEncuentros; j++) { %>
                            <td></td>
                        <% } %>
                        
                        <td class="font-small text-muted"><small><%= index + 1 %>)</small></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <div class="mt-5 d-flex justify-content-between text-center font-small">
            <div style="width: 200px;">
                <div style="border-top: 1px solid #000; padding-top: 5px;">Firma del Formador</div>
            </div>
            <div style="width: 200px;">
                <div style="border-top: 1px solid #000; padding-top: 5px;">Sello del CIIE</div>
            </div>
        </div>
    </div>

</body>
</html>